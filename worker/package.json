{
  "name": "tobuy2-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate-data": "node scripts/migrate-data.mjs"
  },
  "dependencies": {
    "@automerge/automerge-repo": "^2.5.5"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
